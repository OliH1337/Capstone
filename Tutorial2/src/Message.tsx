
//PascalCasing
function Message () {
    // JSX: javaScript XML
    const name = ""
    if (name)
        return <h1> Helllo {name}</h1>;
    else
        return <h1> Hellow World</h1>;
}

export default Message;